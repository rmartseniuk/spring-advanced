server:
  port: 8443

spring:
  profiles:
    active: dev, ssl

audit:
  path: src/main/resources/audit.txt

logging:
  level:
    ROOT: INFO
    org.springframework: INFO
    org.springframework.data: ERROR
    com.epam.martseniuk: DEBUG